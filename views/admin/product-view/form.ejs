<% if (productRecord && productRecord.id) { %>
    <form id="productForm" method="POST" action="/admin/product-view/update/<%= productRecord.id %>">
<% } else { %>
    <form id="productForm" method="POST" action="/admin/product-view/create">
<% } %>
        <div class="form-group row">

            <label class="col-sm-2 col-form-label">User Id</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" name="userId" value="<% if (productRecord.userId) { %><%= productRecord.userId %><% } %>">
            </div>
        </div>
        <div class="form-group row">
            <label class="col-sm-2 col-form-label">Product Id</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" name="productId" value="<% if (productRecord.productId) { %><%= productRecord.productId %><% } %>">
            </div>
        </div>
        <div class="form-group row">
            <label class="col-sm-2 col-form-label">Date</label>
            <div class="col-sm-10">
                <input type="text" class="form-control datepicker" name="date" value="<% if (productRecord.date) { %><%= productRecord.date %><% } %>">
            </div>
        </div>
        <div class="hr-line-dashed"></div>
        <div class="form-group row">
            <div class="col-sm-4 col-sm-offset-2">
                <a href="/admin/product" class="btn btn-white btn-sm">Cancel</a>
                <button class="btn btn-primary btn-sm" type="submit">Save changes</button>
            </div>
        </div>
    </form>
    <% if(req.session.flash) { %>
      <script type="text/javascript">
        $(document).ready(function() {
          toastrDisplay("<%= req.session.flash.type %>", "<%= req.session.flash.message %>");
        });
      </script>
      <% req.session.flash = {}; %> <% }%>
      <script>
        $(document).ready(function() {

          var validator = $("#productForm").validate({
            rules: {
              productName: { required: true },
              price: {
                required: true,

              }
            },
            messages: {
              productName: { required: "product Name is required" },
              price: { required: "price is required" },
            },
          });
          $('.datepicker').datepicker({
              format: 'dd-mm-yy',
          });
        });

      </script>
